---
phase: 01-foundation-spotify-parser
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - tailwind.config.ts
  - postcss.config.mjs
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - src/types/spotify.ts
  - api/index.py
  - requirements.txt
  - vitest.config.mts
  - vitest.setup.ts
autonomous: true
requirements:
  - DEP-01
  - DEP-02

must_haves:
  truths:
    - "Next.js app runs locally on port 3000"
    - "Python health endpoint returns JSON response"
    - "Vitest test runner executes successfully"
    - "TypeScript compiles without errors"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "next"
    - path: "api/index.py"
      provides: "FastAPI health check endpoint"
      exports: ["app"]
    - path: "src/types/spotify.ts"
      provides: "TypeScript types for Spotify data"
      exports: ["SpotifyTrack", "ParsedPlaylist", "ParsedTrack"]
    - path: "vitest.config.mts"
      provides: "Test framework configuration"
      contains: "defineConfig"
    - path: "requirements.txt"
      provides: "Python dependencies"
      contains: "fastapi"
  key_links:
    - from: "package.json"
      to: "vitest.config.mts"
      via: "test script"
      pattern: "vitest"
    - from: "api/index.py"
      to: "/api"
      via: "FastAPI route"
      pattern: "@app.get"
---

<objective>
Create the Portify project foundation: Next.js 15 app with Python serverless on Vercel, TypeScript types for Spotify data, and Vitest test infrastructure.

Purpose: Establish the deployable skeleton that all other plans build upon. DEP-01 and DEP-02 must be verifiable after this plan.
Output: Working Next.js app with Python health endpoint, ready for parser and UI development.
</objective>

<execution_context>
@C:/Users/6120142/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/6120142/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-spotify-parser/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Next.js project with TypeScript and Tailwind</name>
  <files>package.json, tsconfig.json, next.config.ts, tailwind.config.ts, postcss.config.mjs, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css</files>
  <action>
Run `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --no-git --yes` to scaffold Next.js 15 project in current directory.

After scaffolding:
1. Update `src/app/page.tsx` to show a simple "Portify - Spotify to YouTube Music" heading (remove default Next.js content)
2. Verify `package.json` has next, react, typescript dependencies
3. Run `npm run dev` briefly to confirm it starts on port 3000

Note: Using `--no-git` since repo already has git initialized.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; echo "Build successful"</automated>
  </verify>
  <done>Next.js app scaffolded, builds successfully, shows Portify heading</done>
</task>

<task type="auto">
  <name>Task 2: Add Vitest test infrastructure and Spotify types</name>
  <files>vitest.config.mts, vitest.setup.ts, src/types/spotify.ts, package.json</files>
  <action>
1. Install test dependencies:
   `npm install -D vitest @vitejs/plugin-react jsdom @testing-library/react @testing-library/dom @testing-library/jest-dom vite-tsconfig-paths`

2. Create `vitest.config.mts` with jsdom environment, react plugin, tsconfig paths (see RESEARCH.md Pattern: Vitest Configuration)

3. Create `vitest.setup.ts` importing '@testing-library/jest-dom' and cleanup afterEach

4. Add to `package.json` scripts:
   - "test": "vitest"
   - "test:run": "vitest run"

5. Create `src/types/spotify.ts` with all types from RESEARCH.md:
   - SpotifyLibrary, SpotifyTrack (for YourLibrary.json)
   - SpotifyPlaylistExport, SpotifyPlaylist, SpotifyPlaylistItem (for Playlist1.json)
   - ParsedPlaylist, ParsedTrack (unified format for app usage)

Export all types for use by parser in Plan 02.
  </action>
  <verify>
    <automated>npm run test:run 2>&amp;1 | grep -E "(No test files found|passed)" &amp;&amp; echo "Vitest configured"</automated>
  </verify>
  <done>Vitest runs (no tests yet is OK), Spotify types exported from src/types/spotify.ts</done>
</task>

<task type="auto">
  <name>Task 3: Create Python FastAPI health endpoint</name>
  <files>api/index.py, requirements.txt</files>
  <action>
1. Create `requirements.txt` with:
   ```
   fastapi>=0.117.0
   ```

2. Create `api/index.py` with FastAPI app (see RESEARCH.md Pattern 2):
   - Import FastAPI
   - Create `app = FastAPI(title="Portify API", version="0.1.0")`
   - Add GET `/api` returning `{"status": "ok", "service": "portify", "version": "0.1.0"}`
   - Add GET `/api/hello` with optional `name` query param returning greeting

3. Test locally with `npm run dev` then `curl http://localhost:3000/api`

Note: Vercel auto-detects Python functions in api/ directory. No vercel.json needed.
  </action>
  <verify>
    <automated>npm run dev &amp; sleep 5 &amp;&amp; curl -s http://localhost:3000/api | grep -q "ok" &amp;&amp; pkill -f "next dev" &amp;&amp; echo "API responds"</automated>
  </verify>
  <done>FastAPI health endpoint returns JSON at /api, requirements.txt lists fastapi</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npm run build` succeeds without errors
2. `npm run test:run` executes (0 tests OK for now)
3. `npm run dev` starts app on localhost:3000
4. `curl http://localhost:3000/api` returns `{"status":"ok",...}`
5. TypeScript types in src/types/spotify.ts compile without errors
</verification>

<success_criteria>
- DEP-01: App structure ready for Vercel deployment (npm run build succeeds)
- DEP-02: Python serverless function responds at /api endpoint
- Test infrastructure ready for Plan 02 parser tests
- Spotify types defined for Plan 02 to implement against
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-spotify-parser/01-01-SUMMARY.md`
</output>
