---
phase: 04-ux-overhaul-visual-refresh
plan: 01
type: execute
wave: 1
depends_on: [04-00]
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
  - src/components/ui/index.ts
  - src/lib/youtubeMusic.ts
  - src/app/page.tsx
  - src/components/TransferResults.tsx
autonomous: true
requirements: [UX-07, UX-08, UX-09, UX-11]

must_haves:
  truths:
    - "App renders with light background and dark text"
    - "No hydration error in browser console"
    - "No DEBUG logs appear in browser console during transfer"
    - "All UI icons render correctly without broken SVGs"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Light theme CSS variables and utilities"
      contains: "color-scheme: light"
    - path: "src/app/layout.tsx"
      provides: "Clean layout without AnimatedBackground"
      min_lines: 15
    - path: "src/components/ui/index.ts"
      provides: "UI exports without MusicVisualizer"
      exports: ["Checkbox", "GlassCard", "AnimatedNumber", "AnimatedPercentage", "ConfettiCelebration", "useConfetti"]
    - path: "src/lib/youtubeMusic.ts"
      provides: "Clean transfer function without debug logs"
      min_lines: 100
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "CSS import"
      pattern: 'import.*globals.css'
---

<objective>
Transform Portify from dark glassmorphism to clean light theme, fix hydration error, remove debug logs, and clean up dead code.

Purpose: Create inviting, clean visual foundation that aligns with modern app aesthetics (Linear, Notion style) per user decision.
Output: Light-themed app with no console errors or dead code.
</objective>

<execution_context>
@C:/Users/6120142/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/6120142/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ux-overhaul-visual-refresh/04-CONTEXT.md
@.planning/phases/04-ux-overhaul-visual-refresh/04-RESEARCH.md

<interfaces>
<!-- Key files being modified -->

From src/app/layout.tsx (current):
```typescript
import { AnimatedBackground } from "@/components/ui";
// AnimatedBackground causes hydration error - will be removed
```

From src/components/ui/index.ts (current):
```typescript
export { MusicVisualizer } from './MusicVisualizer';
// MusicVisualizer is unused - will be removed from exports
```

From src/lib/youtubeMusic.ts (current):
```typescript
console.log('[DEBUG transferPlaylist] ...');
// Multiple DEBUG logs polluting console - will be removed
```

Lucide icons to use (already installed):
- ArrowRight, Trash2, ExternalLink, ChevronDown, Plus from 'lucide-react'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Light theme CSS + remove AnimatedBackground</name>
  <files>src/app/globals.css, src/app/layout.tsx</files>
  <action>
    1. Rewrite globals.css with light theme CSS variables per RESEARCH.md:
       - --bg-base: #ffffff
       - --bg-subtle: #f8fafc
       - --bg-muted: #f1f5f9
       - --spotify-green: #1db954 (keep)
       - --text-primary: #0f172a
       - --text-secondary: #475569
       - --text-muted: #94a3b8
       - --border: #e2e8f0
       - Subtle shadows (--shadow-sm, --shadow-md, --shadow-lg)
       - Set `html { color-scheme: light; }`

    2. Keep glass utilities but update for light theme:
       - .glass: white background with subtle shadow
       - .glass-strong: slightly darker white with stronger shadow
       - Remove purple/pink particle colors

    3. Update layout.tsx:
       - Remove AnimatedBackground import entirely
       - Remove AnimatedBackground component from body
       - Remove "dark" class from html element

    4. Keep all animation keyframes and utility classes (still used by Framer Motion components).

    NOTE: Per user decision, removing AnimatedBackground entirely (not fixing hydration) since light theme doesn't need particles.
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "(error|Error)" || echo "Build succeeded"</automated>
  </verify>
  <done>
    - globals.css contains light theme variables
    - html has color-scheme: light
    - AnimatedBackground removed from layout.tsx
    - No build errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Clean up dead code and replace inline SVGs</name>
  <files>src/components/ui/index.ts, src/lib/youtubeMusic.ts, src/app/page.tsx, src/components/TransferResults.tsx</files>
  <action>
    1. Remove MusicVisualizer export from src/components/ui/index.ts:
       - Delete line: export { MusicVisualizer } from './MusicVisualizer';
       - Keep AnimatedBackground export for now (file still exists, just not used in layout)

    2. Remove ALL DEBUG console.logs from src/lib/youtubeMusic.ts:
       - Delete all lines containing: console.log('[DEBUG transferPlaylist]
       - Keep error logging (console.error) for actual errors

    3. Replace inline SVGs in src/app/page.tsx:
       - Line 127 arrow SVG -> import and use ArrowRight from lucide-react
       - Line 203 trash SVG -> import and use Trash2 from lucide-react
       - Keep Spotify and YouTube brand logo SVGs (line 122, 132) - brand icons stay custom

    4. Replace inline SVGs in src/components/TransferResults.tsx:
       - External link SVG -> ExternalLink from lucide-react
       - Chevron down SVG -> ChevronDown from lucide-react
       - Plus icon SVG -> Plus from lucide-react
       - (Check actual line numbers as file may differ - search for svg elements)

    Do NOT remove AnimatedBackground.tsx file itself - just unused export. File cleanup can happen in future phase.

    Implementation verification (not user-observable truths):
    - MusicVisualizer is not importable from ui/index.ts
    - All icons use Lucide components (except brand logos)
  </action>
  <verify>
    <automated>npm run test:run 2>&1 | tail -20</automated>
  </verify>
  <done>
    - MusicVisualizer not in ui/index.ts exports
    - No DEBUG logs in youtubeMusic.ts
    - page.tsx uses Lucide ArrowRight and Trash2
    - TransferResults.tsx uses Lucide ExternalLink, ChevronDown, Plus
    - All tests still pass
  </done>
</task>

</tasks>

<verification>
1. Run `npm run dev` - app should render with light background
2. Check browser console - no hydration errors
3. Trigger a transfer flow - no DEBUG logs appear
4. Verify icons render correctly
5. Run `npm run build` - no build errors
</verification>

<success_criteria>
- App displays with white/light gray background and dark text
- No console errors on page load or during interactions
- All icons display correctly using Lucide
- Build passes without errors
- Tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-ux-overhaul-visual-refresh/04-01-SUMMARY.md`
</output>
