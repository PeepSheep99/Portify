---
phase: 04-ux-overhaul-visual-refresh
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/components/TransferBottomBar.test.tsx
  - src/components/YouTubeAuthButton.test.tsx
  - src/components/Toast.test.tsx
  - src/components/TransferProgress.test.tsx
autonomous: true
requirements: [UX-04, UX-05, UX-06]

must_haves:
  truths:
    - "Test files exist for all new components created in Wave 1-2"
    - "Running npm run test:run finds all test stubs"
  artifacts:
    - path: "src/components/TransferBottomBar.test.tsx"
      provides: "Test scaffold for bottom bar component"
      min_lines: 15
    - path: "src/components/Toast.test.tsx"
      provides: "Test scaffold for toast component"
      min_lines: 15
    - path: "src/components/YouTubeAuthButton.test.tsx"
      provides: "Test additions for copy toast behavior"
      min_lines: 20
    - path: "src/components/TransferProgress.test.tsx"
      provides: "Test additions for unified progress calculation"
      contains: "calculateUnifiedProgress"
  key_links: []
---

<objective>
Create test scaffolds for new components before implementation (Nyquist compliance).

Purpose: Ensure automated verification exists before Wave 1-2 execution per Nyquist rule.
Output: Test files ready for TDD-style implementation.
</objective>

<execution_context>
@C:/Users/6120142/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/6120142/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ux-overhaul-visual-refresh/04-CONTEXT.md
@.planning/phases/04-ux-overhaul-visual-refresh/04-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test stubs for new components</name>
  <files>src/components/TransferBottomBar.test.tsx, src/components/Toast.test.tsx</files>
  <action>
    Create test scaffolds with placeholder tests that will be filled in during Wave 1-2 execution.

    **TransferBottomBar.test.tsx:**
    ```typescript
    import { describe, it, expect, vi } from 'vitest';
    import { render, screen, fireEvent } from '@testing-library/react';
    // TransferBottomBar import will be added when component exists

    describe('TransferBottomBar', () => {
      it.todo('renders transfer count in button text');
      it.todo('disables button when not authenticated');
      it.todo('disables button when transferring');
      it.todo('calls onTransfer when clicked');
      it.todo('does not render when count is 0');
    });
    ```

    **Toast.test.tsx:**
    ```typescript
    import { describe, it, expect } from 'vitest';
    import { render, screen } from '@testing-library/react';
    // Toast import will be added when component exists

    describe('Toast', () => {
      it.todo('renders message when visible');
      it.todo('does not render when not visible');
      it.todo('applies animation classes');
    });
    ```

    Note: Use `it.todo()` for placeholder tests. These will be converted to real tests
    when the components are created in Plans 02 and 03.
  </action>
  <verify>
    <automated>npm run test:run -- TransferBottomBar Toast 2>&1 | grep -E "(todo|pending|PASS)" | head -5</automated>
  </verify>
  <done>
    - TransferBottomBar.test.tsx exists with todo placeholders
    - Toast.test.tsx exists with todo placeholders
    - Test runner recognizes the files
  </done>
</task>

<task type="auto">
  <name>Task 2: Add test cases for copy toast and unified progress</name>
  <files>src/components/YouTubeAuthButton.test.tsx, src/components/TransferProgress.test.tsx</files>
  <action>
    Add new test cases to existing test files.

    **YouTubeAuthButton.test.tsx** (add to existing or create if missing):
    ```typescript
    describe('YouTubeAuthButton copy behavior', () => {
      it.todo('shows Copied toast when copy button clicked');
      it.todo('hides Copied toast after 2 seconds');
      it.todo('opens verification URL in new tab on connect');
    });
    ```

    **TransferProgress.test.tsx** (add to existing):
    ```typescript
    describe('unified progress calculation', () => {
      it.todo('calculates matching phase as 0-40%');
      it.todo('calculates creating phase as 40-60%');
      it.todo('calculates adding phase as 60-100%');
      it.todo('displays phase label below percentage');
    });
    ```

    If YouTubeAuthButton.test.tsx does not exist, create it with full structure.
    If TransferProgress.test.tsx exists, add the new describe block.
  </action>
  <verify>
    <automated>npm run test:run -- YouTubeAuthButton TransferProgress 2>&1 | grep -E "(todo|pending|PASS|FAIL)" | head -10</automated>
  </verify>
  <done>
    - YouTubeAuthButton.test.tsx has copy toast test cases
    - TransferProgress.test.tsx has unified progress test cases
    - All tests recognized by runner (may be todo/pending)
  </done>
</task>

</tasks>

<verification>
1. Run `npm run test:run` - all test files should be discovered
2. Todo tests show as pending (not failing)
3. Existing tests still pass
</verification>

<success_criteria>
- 4 test files exist with appropriate test cases
- npm run test:run discovers all test files
- No test failures (todos are pending, not failures)
</success_criteria>

<output>
After completion, create `.planning/phases/04-ux-overhaul-visual-refresh/04-00-SUMMARY.md`
</output>
