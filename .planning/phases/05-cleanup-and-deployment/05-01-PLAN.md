---
phase: 05-cleanup-and-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/MusicVisualizer.tsx
  - src/components/ui/AnimatedBackground.tsx
  - src/app/api/youtube/auth/start/route.ts
  - src/app/page.tsx
  - src/components/TransferProgress.test.tsx
  - src/components/TransferProgress.tsx
  - src/components/YouTubeAuthButton.test.tsx
  - src/lib/spotifyParser.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "ESLint reports 0 errors and 0 warnings"
    - "No unused imports exist in the codebase"
    - "No dead code components exist in src/components/ui/"
  artifacts:
    - path: "src/components/ui/MusicVisualizer.tsx"
      provides: "DELETED - unused component"
    - path: "src/components/ui/AnimatedBackground.tsx"
      provides: "DELETED - unused component"
  key_links:
    - from: "npm run lint"
      to: "exit code 0"
      via: "all issues resolved"
      pattern: "0 problems"
---

<objective>
Fix all ESLint issues and remove dead code from the codebase.

Purpose: Ensure code quality before deployment - no linting errors blocking CI/CD.
Output: Clean codebase with zero ESLint issues.
</objective>

<execution_context>
@C:/Users/6120142/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/6120142/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cleanup-and-deployment/05-RESEARCH.md

Current ESLint issues (from `npm run lint`):
1. ERROR: src/components/ui/MusicVisualizer.tsx:40 - Math.random() impure function in useMemo
2. WARNING: src/app/api/youtube/auth/start/route.ts:36 - 'error' unused
3. WARNING: src/app/page.tsx:3 - 'useCallback' unused
4. WARNING: src/components/TransferProgress.test.tsx:1 - 'vi' unused
5. WARNING: src/components/TransferProgress.tsx:4 - 'CheckCircle' unused
6. WARNING: src/components/YouTubeAuthButton.test.tsx:1 - 'expect' unused
7. WARNING: src/components/YouTubeAuthButton.test.tsx:1 - 'vi' unused
8. WARNING: src/components/YouTubeAuthButton.test.tsx:2 - 'render' unused
9. WARNING: src/components/YouTubeAuthButton.test.tsx:2 - 'screen' unused
10. WARNING: src/components/YouTubeAuthButton.test.tsx:2 - 'fireEvent' unused
11. WARNING: src/lib/spotifyParser.test.ts:10 - 'ParsedPlaylist' unused

Dead code (not imported anywhere):
- src/components/ui/AnimatedBackground.tsx
- src/components/ui/MusicVisualizer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove unused imports from all flagged files</name>
  <files>
    src/app/api/youtube/auth/start/route.ts
    src/app/page.tsx
    src/components/TransferProgress.test.tsx
    src/components/TransferProgress.tsx
    src/components/YouTubeAuthButton.test.tsx
    src/lib/spotifyParser.test.ts
  </files>
  <action>
Remove unused imports from each file:

1. **src/app/api/youtube/auth/start/route.ts** - Remove or prefix 'error' variable with underscore at line 36 (in catch block, rename to `_error` or remove if not needed)

2. **src/app/page.tsx** - Remove 'useCallback' from imports (line 3)

3. **src/components/TransferProgress.test.tsx** - Remove 'vi' from vitest imports (line 1)

4. **src/components/TransferProgress.tsx** - Remove 'CheckCircle' from lucide-react imports (line 4)

5. **src/components/YouTubeAuthButton.test.tsx** - Remove unused imports:
   - Remove 'expect', 'vi' from vitest imports (line 1)
   - Remove 'render', 'screen', 'fireEvent' from @testing-library/react imports (line 2)
   Note: Check if these are actually used in the file - if the test file has real tests using them, keep them.

6. **src/lib/spotifyParser.test.ts** - Remove 'ParsedPlaylist' type import (line 10)

Run `npm run lint` after each file to verify warnings are resolved incrementally.
  </action>
  <verify>
    <automated>cd "C:/Users/6120142/PetProjects/PlaylistCopier" && npm run lint 2>&1 | grep -c "warning" | grep -q "^0$" || npm run lint 2>&1 | grep "warning"</automated>
  </verify>
  <done>All 10 ESLint warnings are resolved. `npm run lint` shows only the MusicVisualizer error.</done>
</task>

<task type="auto">
  <name>Task 2: Delete dead code components</name>
  <files>
    src/components/ui/MusicVisualizer.tsx
    src/components/ui/AnimatedBackground.tsx
  </files>
  <action>
Delete the two unused UI components:

1. **Delete src/components/ui/AnimatedBackground.tsx** - Not imported anywhere in the codebase. Was removed from usage in Phase 4 (04-01 decision: "Removed AnimatedBackground entirely rather than fixing hydration error").

2. **Delete src/components/ui/MusicVisualizer.tsx** - Not imported anywhere in the codebase. Has the React purity error (Math.random in useMemo). Since it's not used, delete instead of fixing.

Before deleting, verify neither file is imported:
```bash
grep -r "AnimatedBackground" src/ --include="*.tsx" --include="*.ts"
grep -r "MusicVisualizer" src/ --include="*.tsx" --include="*.ts"
```

If any imports are found (other than in the files themselves), do NOT delete - fix the import instead.

Also check src/components/ui/index.ts (if it exists) and remove any exports of these components.
  </action>
  <verify>
    <automated>cd "C:/Users/6120142/PetProjects/PlaylistCopier" && npm run lint 2>&1 | grep -c "problem" | grep -q "^0$" && echo "0 problems" || npm run lint</automated>
  </verify>
  <done>Both dead code files deleted. `npm run lint` shows 0 problems (0 errors, 0 warnings).</done>
</task>

</tasks>

<verification>
After both tasks:
1. `npm run lint` exits with code 0 and reports "0 problems"
2. `ls src/components/ui/` does NOT contain AnimatedBackground.tsx or MusicVisualizer.tsx
3. `npm run build` succeeds (no broken imports)
</verification>

<success_criteria>
- ESLint reports exactly 0 errors and 0 warnings
- Dead code components (AnimatedBackground.tsx, MusicVisualizer.tsx) are deleted
- Build still succeeds after cleanup
</success_criteria>

<output>
After completion, create `.planning/phases/05-cleanup-and-deployment/05-01-SUMMARY.md`
</output>
