---
phase: 05-cleanup-and-deployment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/FileDropzone.test.tsx
  - src/components/TransferResults.test.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "All 24 frontend tests pass"
    - "Test assertions match current UI text"
    - "No tests are skipped or commented out"
  artifacts:
    - path: "src/components/FileDropzone.test.tsx"
      provides: "Updated test assertions matching current UI"
      contains: "Upload your Spotify data export"
    - path: "src/components/TransferResults.test.tsx"
      provides: "Updated test assertions matching current UI"
      contains: "text-green-400"
  key_links:
    - from: "npm run test:run"
      to: "exit code 0"
      via: "all tests pass"
      pattern: "Tests.*passed"
---

<objective>
Fix all failing frontend tests by updating assertions to match current UI text and classes.

Purpose: Ensure test suite passes before deployment - tests validate UI contract.
Output: All 24 frontend tests passing.
</objective>

<execution_context>
@C:/Users/6120142/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/6120142/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cleanup-and-deployment/05-RESEARCH.md

Failing tests (from `npm run test:run`):

**FileDropzone.test.tsx (1 failure):**
- "renders dropzone area with instructions" - Looking for "Drag and drop Spotify JSON files here" but UI now shows "Upload your Spotify data export"

**TransferResults.test.tsx (12 failures):**
- Tests expect "playlist created" text but UI shows just playlist name as heading
- Tests expect "tracks added" but UI shows "added"
- Tests expect Tailwind classes like "text-green-600" but UI uses "text-green-400"
- Tests expect "close" button but UI has "Done" button
- Tests expect "transfer another" button but UI has "Continue" button only when batch progress exists

Current UI from TransferResults.tsx:
- Playlist name shown as h2 heading (no "playlist created" prefix)
- Stats: "{tracksAdded}" with "added" label, "{tracksFailed}" with "failed" label
- Match rate colors: text-green-400 (>80%), text-yellow-400 (50-80%), text-red-400 (<50%)
- Button text: "Done" (single playlist) or "Continue" (batch with more playlists)
- No separate "close" or "transfer another" buttons

Current UI from FileDropzone.tsx:
- Main text: "Upload your Spotify data export"
- Sub text: "Drop files or click to browse"
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix FileDropzone.test.tsx assertions</name>
  <files>src/components/FileDropzone.test.tsx</files>
  <action>
Update the "renders dropzone area with instructions" test to match current UI:

**Before:**
```tsx
expect(screen.getByText('Drag and drop Spotify JSON files here')).toBeInTheDocument();
expect(screen.getByText('or click to select files')).toBeInTheDocument();
```

**After:**
```tsx
expect(screen.getByText('Upload your Spotify data export')).toBeInTheDocument();
expect(screen.getByText('Drop files or click to browse')).toBeInTheDocument();
```

The UI was updated during Phase 4 to use simpler, cleaner copy.
  </action>
  <verify>
    <automated>cd "C:/Users/6120142/PetProjects/PlaylistCopier" && npm run test:run -- --filter=FileDropzone 2>&1 | tail -20</automated>
  </verify>
  <done>FileDropzone.test.tsx passes all 5 tests.</done>
</task>

<task type="auto">
  <name>Task 2: Fix TransferResults.test.tsx assertions</name>
  <files>src/components/TransferResults.test.tsx</files>
  <action>
Update TransferResults tests to match current UI. Read the current component first to understand exact text/classes.

**Key changes needed:**

1. **"renders success banner with playlist name"** test:
   - Remove assertion for "playlist created" text (no longer exists)
   - Keep heading assertion for playlist name

2. **"renders track counts"** test:
   - Change "tracks added" to "added" (UI uses shorter label)
   - Keep "failed" assertion as-is (matches)

3. **Match rate color tests** (3 tests):
   - Change "text-green-600" to "text-green-400"
   - Change "text-yellow-600" to "text-yellow-400"
   - Change "text-red-600" to "text-red-400"

4. **"toggles expansion on repeated click"** test:
   - The button accessible name uses "Matched tracks" with capital M
   - Update to match: `screen.getByRole('button', { name: /matched tracks/i })`

5. **Close button tests** (2 tests):
   - The component has a "Done" button, not "close" button
   - Update: `screen.getByRole('button', { name: /done/i })`
   - Remove "transfer another" test OR update to test batch mode Continue button

6. **"handles result with all tracks matched"** test:
   - Verify 100% assertion still works with AnimatedPercentage component

7. **Accessibility tests**:
   - Update close button label test to look for "Done" not "close"

Read the actual component output carefully. Use `screen.debug()` if needed to see what's rendered.
  </action>
  <verify>
    <automated>cd "C:/Users/6120142/PetProjects/PlaylistCopier" && npm run test:run -- --filter=TransferResults 2>&1 | tail -30</automated>
  </verify>
  <done>TransferResults.test.tsx passes all 19 tests.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `npm run test:run` exits with code 0
2. All 24 tests pass (5 FileDropzone + 19 TransferResults)
3. No tests skipped or commented out
</verification>

<success_criteria>
- All frontend tests pass (24 total in these two files)
- Test assertions accurately reflect current UI text and classes
- No tests are marked as `.skip` or `.todo`
</success_criteria>

<output>
After completion, create `.planning/phases/05-cleanup-and-deployment/05-02-SUMMARY.md`
</output>
