---
phase: 03-ui-polish-ux-refinements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/components/TransferProgress.tsx
  - src/components/FileDropzone.tsx
  - src/components/PlaylistList.tsx
  - src/components/YouTubeAuthButton.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Progress bar shows clean circular progress without music visualizer bars"
    - "All common icons use Lucide React instead of inline SVGs"
    - "Icons are consistent size and style across the app"
  artifacts:
    - path: "src/components/TransferProgress.tsx"
      provides: "Clean progress UI with Lucide icons"
      contains: "import.*lucide-react"
    - path: "src/components/FileDropzone.tsx"
      provides: "Upload icon from Lucide"
      contains: "import.*lucide-react"
    - path: "src/components/PlaylistList.tsx"
      provides: "Music/playlist icons from Lucide"
      contains: "import.*lucide-react"
  key_links:
    - from: "package.json"
      to: "lucide-react"
      via: "dependency installation"
      pattern: "lucide-react"
---

<objective>
Add Lucide React icon library and simplify progress bar by removing music theme.

Purpose: Consistent iconography throughout the app and cleaner progress UI as requested.
Output: Updated components using Lucide icons, simplified TransferProgress without MusicVisualizer.
</objective>

<execution_context>
@C:/Users/6120142/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/6120142/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ui-polish-ux-refinements/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Lucide React and replace icons throughout app</name>
  <files>package.json, package-lock.json, src/components/TransferProgress.tsx, src/components/FileDropzone.tsx, src/components/PlaylistList.tsx, src/components/YouTubeAuthButton.tsx</files>
  <action>
1. Install lucide-react:
   ```bash
   npm install lucide-react
   ```

2. Update TransferProgress.tsx:
   - Remove `import { MusicVisualizer } from '@/components/ui';`
   - Add `import { Search, ListPlus, Music2, CheckCircle, XCircle, Loader2 } from 'lucide-react';`
   - Remove the MusicVisualizer JSX from the progress circle area (lines ~173-177)
   - Remove the gradient glow div, replace with simple `<div className="absolute inset-4 rounded-full bg-[#1db954]/20 blur-xl" />`
   - Change the gradient stroke to solid Spotify green: `stroke="#1db954"` instead of `stroke="url(#progressGradient)"`
   - Remove the `<defs>` gradient definition (lines ~154-161)
   - Replace emoji icons in phaseConfig with Lucide components:
     ```typescript
     const phaseIcons = {
       matching: Search,
       creating: ListPlus,
       adding: Music2,
     };
     ```
   - Update phase indicator to render Lucide icon instead of emoji:
     ```tsx
     const PhaseIcon = phaseIcons[progress.phase];
     // In JSX:
     <PhaseIcon className="w-5 h-5 text-white/60 mr-2" />
     ```
   - In error state, replace inline SVG with `<XCircle className="w-8 h-8 text-red-400" />`
   - In complete state, replace inline SVG with `<CheckCircle className="w-8 h-8 text-green-400" />`

3. Update FileDropzone.tsx:
   - Add `import { Upload } from 'lucide-react';`
   - Remove the `<MusicVisualizer>` import and JSX (background visualizer)
   - Replace the upload icon inline SVG with `<Upload className="w-8 h-8 text-white/70" />` (or text-[#1db954] when isDragActive)

4. Update PlaylistList.tsx:
   - Add `import { Heart, Music, ChevronDown, ArrowRight, Loader2, ListMusic } from 'lucide-react';`
   - In TrackItem: no icon changes needed (keeps gradient placeholder)
   - In PlaylistCard:
     - Replace heart SVG with `<Heart className="w-8 h-8 text-white/80" fill="currentColor" />`
     - Replace music note SVG with `<Music className="w-8 h-8 text-white/60" />`
     - Replace chevron SVG with `<ChevronDown className="w-4 h-4" />`
     - Replace arrow SVG with `<ArrowRight className="w-4 h-4" />`
     - Replace spinner div with `<Loader2 className="w-4 h-4 animate-spin" />`
   - In section headers:
     - Replace liked songs heart SVG with `<Heart className="w-4 h-4 text-white" fill="currentColor" />`
     - Replace playlists box SVG with `<ListMusic className="w-4 h-4 text-white" />`
   - In empty state: Replace music note SVG with `<Music className="w-8 h-8 text-white/30" />`

5. Update YouTubeAuthButton.tsx:
   - Add `import { ExternalLink, Copy, Check } from 'lucide-react';`
   - Keep YouTube brand icon as inline SVG (brand icons should stay custom)
   - Replace external link SVG with `<ExternalLink className="w-4 h-4" />`
   - Replace copy SVG with `<Copy className="w-5 h-5 text-white/50 group-hover:text-white/80 transition-colors" />`
   - In connected state checkmark, keep the motion.svg for the animated path (Lucide doesn't animate paths)
  </action>
  <verify>
    <automated>npm run build && npm run test:run -- --reporter=dot</automated>
  </verify>
  <done>
- lucide-react installed in package.json
- TransferProgress uses solid green progress circle without MusicVisualizer
- Phase icons show Search/ListPlus/Music2 Lucide icons
- FileDropzone uses Upload icon from Lucide (no background visualizer)
- PlaylistList uses Heart/Music/ChevronDown/ArrowRight/Loader2/ListMusic from Lucide
- YouTubeAuthButton uses ExternalLink/Copy from Lucide
- Build succeeds with no TypeScript errors
  </done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- `npm run test:run` passes
- Visual inspection: Progress bar shows clean circle without visualizer bars
- Visual inspection: Icons are consistent Lucide style throughout
</verification>

<success_criteria>
- Lucide React installed and tree-shakable imports used
- MusicVisualizer removed from TransferProgress and FileDropzone
- Progress circle uses solid #1db954 stroke
- All inline SVGs replaced with Lucide equivalents (except brand icons)
- No TypeScript errors, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/03-ui-polish-ux-refinements/03-01-SUMMARY.md`
</output>
